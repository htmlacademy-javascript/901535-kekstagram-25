const arrayPicture = document.querySelectorAll('.picture');

const createWindow = () => {
  const userWindow = document.querySelector('.big-picture');
  userWindow.classList.remove('hidden');

  userWindow.querySelector('.big-picture__img')
};

export {createWindow}
